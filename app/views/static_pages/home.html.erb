<div class="container">
<% if logged_in? %>
  <div class="container">
    <section class="row">
      <aside class="col-lg-4">
        <div class="p-3 tweet-form shadow mt-5">
          <section class="user_info">
            <%= render 'shared/user_info' %>
          </section>
          <section class="tweet_form">
            <%= render 'shared/tweet_form' %>
          </section>
        </div>
      </aside>
      <div class="col-lg-8">
          <div class="row">
            <%= form_with url: tweet_search_path, method: :get, class: "form-inline d-block text-right col-xl-12  mt-4 mt-lg-0", local: true do |f| %>
              <input class="form-control m-0 mr-1" type="search" placeholder="Search" aria-label="Search" name="keyword" id="keyword">
              <button class="btn search-btn mt-2 m-md-0" type="submit">Search</button>
            <% end %>
          </div>
          <% if @search == 'search'%>
            <h4 class="mb-3"><%= @feed_count %>件ヒットしました</h4>
          <% else %>
            <h4 class="mb-3">みんなのツイート (<%= @feed_count %>)</h4>
          <% end %>

        <%= render 'shared/feed' %>
      </div>
    </section>
  </div>
<% else %>
  <h1>Fodeli Onlineとは？</h1>
  <p class="text-center mb-5">フードデリバリー配達員が仕事に関する情報交換や、同業種の繋がり作ることができるサービスです。</p>
  <div class="row  mb-5">
    <p class="col-6 d-flex align-items-center">・仕事をしてる時、「注文来ないな〜。稼いでる人はどんな風に仕事しているんだろう？」<br>
      ・もっと同業種の知り合いが欲しいな〜 一人でずっと仕事してても、寂しくなってくる....<br>
      こんな風に思ったことはありませんか？
    ​</p>
    <%=image_tag("derivari_man.jpg", :alt => "",class: "col-6 border rounded shadow p-0")%>
  </div>
  <div class="row mb-5">
    <%=image_tag("chat.jpg", :alt => "",class: "col-6 border rounded shadow p-0")%>
    <p class="col-6 d-flex align-items-center">フーデリオンラインでは、地域別のオンラインチャットで配達に関する情報交換をすることができます。「 Uber Eats、出前館、フードパンダ、wolt 、menuなど」多くのサービスに対応しています。

    ​</p>
  </div>
  <div class="row">
    <p class="col-6 d-flex align-items-center">オフ会の開いたり参加したりもできます。実際に顔を合わせて交流を深めましょう！
    ​</p>
    <%=image_tag("off.jpg", :alt => "",class: "col-6 border rounded shadow p-0")%>
  </div>
<% end %>
</div>
