<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <!-- 第一条件： ログインしてるユーザーが管理者であり、表示ユーザーがログインしているユーザーでない場合に削除リンクを表示する -->
  <!-- 第二条件： ログインしてるユーザーが管理者ではなく、表示されているユーザーが自身であれば削除リンクを表示する -->
  <% if (current_user.admin? && current_user != user) || (!current_user.admin? && current_user == user) %>
  | <%= link_to "削除", user_path(user), method: :delete,
                                data: { confirm: "本当に削除しますか？" } %>
  <% end %>
</li>