<% provide(:title, @title) %>
<div class="row align-items-start mt-5">
  <aside class="col-lg-4 p-4 user-picture-box shadow">
    <section class="user_info">
      <div class="d-flex align-items-center mb-4 pb-2 border-bottom">
        <%= image_tag @user.image.url,class: "profile-image" %>
        <h3 class="m-0 ml-4"><%= @user.name %></h3>
      </div>
      <p><%= link_to "プロフィール", @user %></p>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
      <% if @users.any? %>
        <div class="user_avatars">
          <% @users.each do |user| %>
          <%= link_to user do%>
            <%= image_tag user.image.url,class: "profile-image profile-image-follow" %>
          <% end %>
          <% end %>
        </div>
      <% end %>
    </section>
  </aside>
  <div class="col-lg-8">
    <h3 class="p-3"><%= @title %></h3>
    <% if @users.any? %>
      <ul class="users follow">
        <%= render @users %>
      </ul>
      <%= paginate @users %>
    <% end %>
  </div>
</div>