<% provide(:title, "参加中チャット") %>
<h4 class="chat-user-name p-3 d-inline-block"><i class="fas fa-comment mr-2 chat-head"></i>参加中チャット</h4>
<div class="row mt-3">
  <section class="col-lg-8">
    <ul class="list-group">
      <% if @chat_rooms.present? %>
        <%= render @chat_rooms %>
      <% end %>
    </ul>
    <%= paginate @chat_rooms %>
  </section>
</div>