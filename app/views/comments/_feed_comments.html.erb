<div class="row">
  <div class="col-md-12">
    <h3 class="title-comment">コメント (<%= "#{@tweet.comments.count}" %>)</h3>
  </div>
</div>
<% if @comments.any? %>
  <ul class="comments">
    <%= render @comments %>
  </ul>
  <%= paginate @comments %>
<% end %>