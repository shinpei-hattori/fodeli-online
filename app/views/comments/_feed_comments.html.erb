<div class="row">
  <div class="col-md-12">
    <h3 class="title-comment mb-3">コメント (<%= "#{@tweet.comments.count}" %>)</h3>
  </div>
</div>
<% if @comments.any? %>
  <ul class="comments p-0">
    <%= render @comments %>
  </ul>
  <%= paginate @comments %>
<% end %>